<?HH

class KosarajuTest extends \PHPUnit_Framework_TestCase
{
	public function testCanGetTotalScc()
	{
		/*
		$stronglyConnected = Map{
			0 => Vector{1},
			1 => Vector{0}
		};

		$dfs = new \HackFastAlgos\Kosaraju($stronglyConnected, 0);
		$dfs->run();

		$this->assertSame(1, $dfs->getTotalScc());

		$stronglyConnected = Map{
			0 => Vector{1},
			1 => Vector{1, 2},
			2 => Vector{1}
		};

		$dfs = new \HackFastAlgos\Kosaraju($stronglyConnected, 0);
		$dfs->run();

		$this->assertSame(1, $dfs->getTotalScc());
		*/
	}
}
